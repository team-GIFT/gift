import styled from 'styled-components';
import { px2rem } from '@/styles/utils';
export const StyledCommonWrapper = styled.div`
  display: block;
  max-width: ${px2rem(1070)};
  padding: 0 ${px2rem(15)};
  margin: 0 auto;
`;
